const EMOJIS = [`smile`, `sleeping`, `puke`, `angry`];
const FILM_COUNT_PER_STEP = 5;
const MAX_FILMS_NUMBER = 25;
const MAX_EXTRA_FILM_CARD_NUMBER = 2;
const ESC_KEY = `Escape`;

const Mode = {
  OPENED: `OPENED`,
  CLOSED: `CLOSED`
};

const SortType = {
  DEFAULT: `default`,
  DATE: `date`,
  RATING: `rating`
};

export {EMOJIS, FILM_COUNT_PER_STEP, MAX_FILMS_NUMBER, MAX_EXTRA_FILM_CARD_NUMBER, ESC_KEY, Mode, SortType};
